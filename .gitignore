node_modules
dist
.env
.DS_Store
coverage
*.log
/.idea
/.vscode

# Fichier ngrok avec identifiants de démo (ne pas committer)
ngrok-demo-policy.yml

# Dump base (données sensibles)
backup_fab.sql
backups/

# Documentation cybersécurité (exclue du dépôt)
docs/cybersecurite/

# Sortie d'audit locale (ne pas committer)
api-audit.err
.codex_known_hosts

# Artefacts TypeScript (build incrémental)
*.tsbuildinfo

# Sources GIMP / fichiers de travail graphique (hors livrable runtime)
*.xcf
